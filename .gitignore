# Build artefacts of other projects. Both are already zip archives, so git can
# neither compress nor delta them. See readme.md for how to obtain each.

# MolWURCS 0.12.2 -- gitlab.com/glycoinfo/molwurcs
molwurcs.jar

# WURCSFilter 0.6.1 -- gitlab.com/glycoinfo/wurcsfilter
WURCSFilter.jar

# Compiled from the .java sources by analyze_rescode.sh
*.class
